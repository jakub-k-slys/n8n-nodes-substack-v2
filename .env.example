# Example environment variables for E2E testing
# Copy this file to .env and fill in your credentials to run E2E tests

# Your Substack API key - get this from your Substack account
SUBSTACK_API_KEY=your-api-key-here

# Your Substack publication hostname (optional - only needed for real API tests)
SUBSTACK_HOSTNAME=yoursite.substack.com

# Note: The E2E tests in this project use mocked API responses by default
# These environment variables are only needed if you want to run tests against the real API